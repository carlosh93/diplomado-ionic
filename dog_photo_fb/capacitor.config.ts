import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'dog_photo_fb',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
